name: Ping API

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
  push:   # 👈 YE ADD KARNA ZARURI HAI

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: curl -sSf -I https://api.gaongully.com/api/health
