name: Ping API

on:
  schedule:
    - cron: "*/5 * * * *"   # har 5 min
  workflow_dispatch:        # manual run bhi kar sakte ho

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: |
          curl -I https://api.gaongully.com/api/health
